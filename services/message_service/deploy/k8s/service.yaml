apiVersion: v1
kind: Service
metadata:
  name: msg-service
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing
spec:
  type: LoadBalancer
  selector:
    app: msg-service
  ports:
    - port: 8080
      targetPort: 8080